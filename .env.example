# Puerto de la app en produccion (Docker)
APP_PORT=8080

# Puerto del servidor de desarrollo (perfil dev)
DEV_PORT=4200

# Puerto del API de persistencia (Docker)
API_PORT=3000

# Gemini IA para asistente de profesores/admin
GEMINI_API_KEY=
GEMINI_MODEL=gemini-flash-lite-latest
